Create a 60x40x20mm rectangular block. Cut a 20mm long, 8mm wide slot
into the left face of the block (the face at -X), centered on that
face, cutting all the way through to the opposite face.

Create a 100mm diameter cylinder, height 20mm. Add a 6mm radius hole near the edge, then arrange 8 copies of that hole in a circular pattern around the cylinder's center axis

Create a lofted part transitioning from a 50x50mm square base to a 20mm radius circular top, over a height of 80mm.

Create a 5mm radius tube that sweeps along a curved path from (0,0,0) to (60,40,30), passing through a couple of intermediate points to give it a gentle curve.

Create a revolved ring: a rectangular profile 15mm wide by 30mm tall, offset 40mm from the center axis, revolved 360° around the Y axis.